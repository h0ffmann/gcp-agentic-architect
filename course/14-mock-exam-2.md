# 14 — Mock exam 2

Timed, 60 questions, 120 minutes: `just quiz --build-mock 2` (fails loudly if any section lacks
unused questions — write more first), then `just quiz --mock 2 --minutes 120`.

After scoring: `just quiz --stats` → predicted score and three weakest objectives → one `--weak`
session per weak objective before the next lesson. Record the score in `STUDY-LOG.md`.

Final review sheet: re-read every lesson's 'On the exam' block in one sitting, then the flashcards only.
