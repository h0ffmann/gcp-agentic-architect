# 13 — Mock exam 1

Timed, 60 questions, 120 minutes: `just quiz --build-mock 1` (fails loudly if any section lacks
unused questions — write more first), then `just quiz --mock 1 --minutes 120`.

After scoring: `just quiz --stats` → predicted score and three weakest objectives → one `--weak`
session per weak objective before the next lesson. Record the score in `STUDY-LOG.md`.

Weak-spot review comes before Section 1 on D12.
