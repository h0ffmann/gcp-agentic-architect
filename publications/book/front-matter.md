# About this book {.unnumbered}

Every `(v)` mark in this book was checked on the date shown next to it; `⚠` marks were not.
Product names in this family change quickly — re-verify before relying on a number or a name.

Built from `course/` by `scripts/build_pdf.sh` (`just book`, or `nix build .#book` in the Nix
sandbox); CI commits the PDF to `pdf/` on `main`. The source of truth is the repository:
<https://github.com/h0ffmann/gcp-agentic-architect>.
